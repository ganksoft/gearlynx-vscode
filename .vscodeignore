# Sources and build config (the compiled output in out/ is what ships)
src/**
tsconfig.json
eslint.config.mjs

# Tooling and dev files
.github/**
.vscode/**
.gitignore
.vscodeignore
package-lock.json
AGENTS.md

# Source maps and TS declaration files
out/**/*.map
out/**/*.d.ts

# README screenshots (Marketplace renders them from the GitHub raw URL, no need
# to ship them); keep only the extension icon that package.json references
images/**
!images/icon.png

# Misc
**/.DS_Store
